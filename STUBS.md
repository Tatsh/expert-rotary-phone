# STUBS — systematic inventory of incomplete reconstructions
# Generated by scanning for stub markers. Every entry is a place where the
# reconstruction is NOT complete. Remove an entry only when its body is real code.

## (a) (void)local casts — value fetched, consuming logic stubbed
Project/System/src/Aep/AepManager.mm:69:    (void)path;
Project/Game/Note/PlayJudge.mm:211:    (void)audio;   // the milestone SEs play through the play-data voice handles
Project/System/src/Task/AcMainTask.mm:30:    (void)aep; (void)nm;
Project/System/src/Task/PlayTask.mm:86:        (void)nm; (void)audio;

## (b) comment-only / seam function bodies
Project/StoreMainViewController.mm:74:// the stretchable pack-cell backgrounds (store_pack_bg_0/1). RECONSTRUCTION DEFERRED:
Project/System/src/Task/AcMainTask.mm:7://  its full state machine is a large deferred unit — this carries the ctor and the
Project/System/src/Task/AcMainTask.mm:25:// the arcade result screen. The full ~24 KB per-state logic is a deferred unit; the
Project/System/src/Task/AcMainTask.mm:31:    // TODO(deferred): the arcade select+play state machine (FUN_00099d18). Drives
Project/System/src/Task/MenuMainTask.mm:48:// rectangles, SE ids). Modelled as a seam; the layer + rect setup is its own unit.
Project/System/src/Aep/AepManager.mm:67:    // handled by the engine texture/index loader; each frame is appended to the
Project/Game/Purchase/PurchaseManager.m:343:                break;   // Purchasing / Deferred: nothing to do
Project/Game/Note/PlayJudge.mm:30:// The play data is the standard-mode MainTask (not yet reconstructed as a whole).
Project/Game/Note/PlayJudge.mm:202:        // 16.16/NEON geometry is reconstructed there, not here.)

## (c) empty-brace / placeholder bodies (manual review)
Project/System/src/Task/MenuMainTask.mm:48:// rectangles, SE ids). Modelled as a seam; the layer + rect setup is its own unit.
Project/System/src/Task/MenuMainTask.mm:50:    // (Loads the menu Aep layers into +0x28/+0x2c/+0x30 and fills the button rects.)
Project/System/src/Task/MenuMainTask.mm:207:    // Per-frame menu UI update + draw (Ghidra tail: FUN_0002c924 / FUN_0006d428).
Project/System/src/Task/AcMainTask.mm:7://  its full state machine is a large deferred unit — this carries the ctor and the
Project/System/src/Task/AcMainTask.mm:25:// the arcade result screen. The full ~24 KB per-state logic is a deferred unit; the
