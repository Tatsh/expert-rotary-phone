/HANDOFF.md
.decompile/

# CMake / build outputs
/build/
CMakeCache.txt
CMakeFiles/
# Stray root Theos artifacts (the supported Theos project lives in theos/, tracked).
/Makefile
/control
/Resources/Info.plist
/.theos/
/packages/
/obj/
/.audit/
.audit/scratch/

# CI build outputs (see .gitlab-ci.yml): ios-cmake clone, .ipa packaging, Theos install
# + its per-project build dirs (theos/ is the project dir when `make -C theos` runs).
/.ios-cmake/
/.theos-install/
/Payload/
*.ipa
/theos/.theos/
/theos/obj/
/theos/packages/

# Generated sugoroku dialogue (copyrighted content, produced at configure time)
sugoroku_chara_msg.generated.inc
sugoroku_chara_msg.bin
**/generated/
